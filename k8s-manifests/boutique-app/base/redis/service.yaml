apiVersion: v1
kind: Service
metadata:
  name: redis-cart
  namespace: boutique
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
  clusterIP: None  # Headless — StatefulSet needs DNS per pod
